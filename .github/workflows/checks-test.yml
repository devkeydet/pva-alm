name: checks-test #updates

on:
  pull_request:
    
jobs:
  checks-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # - id: add-check
      #   name: add-check
      #   uses: LouisBrunner/checks-action@v1.2.0
      #   with:
      #     token: ${{ secrets.GITHUB_TOKEN }}
      #     name: devkeydet-test-check
      #     status: queued
      #     details_url: https://www.microsoft.com
      # - name: echo-check-id
      #   run: |
      #     echo "check_id: ${{ steps.add-check.outputs.check_id }}"
      # - name: add-check-pwsh
      #   shell: pwsh
      #   run: |
      #     curl -X POST -H "Accept: application/vnd.github.v3+json" -H "Authorization: token ${{ github.token }}" https://api.github.com/repos/${{ github.repository }}/check-runs -d '{"name":"pwsh","head_sha":"${{ github.sha }}"}'
      - name: run scriot
        uses: actions/github-script@v3
        with:
          github-token: ${{ github.token }}
          result-encoding: string
          script: |
            let sum = 0;
            const numbers = [65, 44, 12, 4];
            numbers.forEach(myFunction);

            function myFunction(item) {
              sum += item;
            }

            return sum
      - name: Get result
        run: |
          echo "sum: ${{steps.set-result.outputs.result}}"
            