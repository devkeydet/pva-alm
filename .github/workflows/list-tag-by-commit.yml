name: list-tag-by-commit

on:
  workflow_dispatch:

jobs:
  list-tag-by-commit:
    runs-on: ubuntu-latest

    steps:      
      - uses: actions/checkout@v2

      - run: |
          git fetch --all --tags
