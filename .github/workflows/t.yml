name: t

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: ./.github/actions/replace-tokens-with-secrets
      with:
        secrets: ${{ toJSON(secrets) }}
        files: '["src/pvaalm/deploymentSettings.json"]'
